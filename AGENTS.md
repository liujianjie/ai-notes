# 项目协作说明

- 交流语言使用中文。
- 涉及 IBKR 交易许可的手动操作前，先阅读 `docs/1.1 ibkr-trading-permissions-setup.md`；配套指引已就位。
- 不在仓库、对话、日志或提交记录中保存或回显密码、令牌、API Key、Cookie 等密钥。

## 上下文读取边界

- 新对话启动或普通问答时，不主动扫描、枚举或预读仓库内的 Markdown 文档。
- 只有当用户明确指向某个文件/目录，或当前任务必须依赖现有文档内容时，才按需读取最少量相关文件。
- 为新文档确定序号时，默认只查看目标目录的文件名，不读取旧文档正文；除非用户要求基于旧文内容继续、比较或改写。

## 文档命名与归档(适用本仓库所有 md)

- 一律用 `N.M 标题.md` 编号。`N` 是主题模块,`M` 是模块内顺序,**同一模块的文档共用一个 N、序号挨在一起**;新主题开新的一级序号,不要挂到已有模块下当子号。
- `0.x` 留给原始 prompt / 提示词记录。带强时点属性的(具体价格、概率赔率、当期财报)在序号后加日期,如 `3.1 2026-07-25 标题.md`;纯认知/方法类不加。
- 一级序号大致按建档时间排,但模块归属优先于时间——后写的续篇仍挂回原模块。
- 目录归属:`AIStocking/` 投资标的与择时判断,`AIKnowlege/` 方法论与产业认知,`AIStockAnalayDocs/` 单公司深度与策略草稿,仓库根放与投资无关的工程/工具笔记。
- 新建前先看目标目录现有序号,判断是挂进已有模块还是开新模块;判断不了就问。
- 改名或移动后必须全仓 grep 旧文件名、修掉所有相对路径引用;文件名含空格时链接里写 `%20`;用 `git mv` 保留历史。
